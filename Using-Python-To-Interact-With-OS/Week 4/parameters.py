#!/usr/bin/env python3

import sys

print(f"SYS ARGV LIST: {sys.argv}")

# Exit Status environment variable = $?
#    Returns 0 if the last command was successful.
#    Returns 1 if the last command was unsuccessful.

# [wc] command counts the number of lines, words, characters in the file.